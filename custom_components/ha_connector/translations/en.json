{
    "config": {
        "abort": {
            "single_instance_allowed": "Only a single instance is necessary."
        },
        "step": {
            "user": {
                "description": "Enter the setting values(app_url, app_id, access_token) to install the HA-Connector.",
                "title": "HA-Connector Settings",
                "data": {
                    "app_url": "App Url",
                    "app_id": "App ID",
                    "access_token": "Access Token"
                }
            }
        }
    },
    "title": "HA-Connector"
}
